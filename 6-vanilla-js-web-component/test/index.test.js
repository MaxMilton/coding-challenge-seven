// FIXME: The web component API is not supported in jsdom; if I have more time
// figure out a way to make a polyfill work.

'use strict';

// const Banner = require('../src/index.js');

describe('Banner component', () => {
  it.skip('renders correctly', () => {
    expect.assertions(1);
  });
});
